tunnel: a28f1256-d7d8-417f-9d4c-fe7090a715be
credentials-file: /home/loki/.cloudflared/a28f1256-d7d8-417f-9d4c-fe7090a715be.json

ingress:
  # API subdomain → NestJS (via Nginx)
  - hostname: api.thinkblooms.in
    service: http://localhost:80

  # Main domain → Next.js (via Nginx)
  - hostname: thinkblooms.in
    service: http://localhost:80

  - hostname: www.thinkblooms.in
    service: http://localhost:80

  # Required catch-all
  - service: http_status:404
